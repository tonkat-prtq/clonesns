<h1>すべての投稿</h1>
<table>
  <tr>
    <th>内容</th>
  </tr>

  <% @homes.each do |home| %>
  <tr>
    <td><%= home.content %></td>
    <td><%= link_to '詳細を確認する', home_path(home.id) %></td>
  </tr>
  <% end %>
</table>

<%= link_to '作成画面へ', new_home_path %>
